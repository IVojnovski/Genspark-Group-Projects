import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class Ch2_test {
	//need to add unit tests later
	//if this file is causing conflicts when trying to run comment out the entire class and run again
	//Most likely caused because you dont have the JUnit library installed

	@Test
	void test() {
		fail("Not yet implemented");
	}

}
